import { FC } from 'react';

interface SearchQueryFormProps {}

const SearchQueryForm: FC<SearchQueryFormProps> = () => {
  return (
    <div>
      SearchQueryForm
    </div>
  );
};

export default SearchQueryForm;
